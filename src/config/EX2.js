let fruit = ['apple', 'banana', 'orange'];
console.log('Задание 2:');
console.log('Вывод элементов через ForEach:');
fruit.forEach(function(fruit, index, array) {
  console.log(`Элемент #${index}: ${fruit}`);
});